#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    
    while (T--) {
        int X, Y;
        cin >> X >> Y;
        
        int games_needed = (Y - X + 7) / 8;
        cout << games_needed << endl;
    }
    
    return 0;
}
